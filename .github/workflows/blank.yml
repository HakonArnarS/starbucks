name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Run a one-line script
        run: npm install npx
      - name: Run a multi-line script
        run: |
          npx quasar built -m pwa,
          echo test, and deploy your project.
